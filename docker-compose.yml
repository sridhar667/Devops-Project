version: '3.8'
services:
  web:
    image: react-app
    user: root
    build: .
    ports:
      - "8081:81"
    volumes:
      - jenkins_home:/var/jenkins_home
      - /var/run/docker.sock:/var/run/docker.sock  # <-- This is critical
volumes:
  jenkins_home:
