version: '3.8'
services:
  web:
    image: react-app
    build: .
    ports:
      - "80:81"
    volumes:
      - jenkins_home:/var/jenkins_home
      - /var/run/docker.sock:/var/run/docker.sock  # <-- This is critical
volumes:
  jenkins_home:
